# Handoff — 大盘扫描辅助指标三连修

**Date**: 2026-07-14
**Handoff reason**: 后端改完未生效，需重启并重算数据后验证前端展示

## Context

- 一步九万里在暴跌日发现大盘扫描页面三个指标数据可疑，逐项核实后确认三处 bug
- 全部修改已完成，但 Flask 服务未重启（僵尸进程问题），数据库新列未迁移，市场健康数据未重算
- 前端改动已在 `web/market-scan/index.html` 中，但浏览器可能缓存

## 三个 Bug 及修复

| 指标 | 问题 | 修复 | 状态 |
|------|------|------|------|
| 涨跌家数比 | 5日均值 0.75 掩盖当日 0.17（803涨/4754跌） | 卡片双值显示 `0.75(5日)/0.17(当日)`，后端返回 `today` 字段 | 后端已改，DB 需迁移 + 重算 |
| 放量突破 | 110只无明细，暴跌日突破数可疑 | 点击弹窗表格加「成交金额」「MA50均量」两列，可逐只核查 | 后端已改，前端已改 |
| 恐慌贪婪 | 三个子指标均为恐慌方向（高值=恐慌），但标注为"贪婪" | `100 - 恐慌原始分` 反转，75.8→24.2（极度恐慌） | 后端已改，DB 存旧值 75.8 需重算 |

## 涉及修改的文件

| 文件 | 改动 |
|------|------|
| `src/scanners/market_health.py` | `compute_ad_ratio` 返回双值；`compute_fear_greed` 反转公式（`100-fear_raw`）；`score_fear_greed` 阈值改为 [10,8,6,4,0]；`market_breakout_daily` 加 `vol_ma50`；`market_health_daily` 加 `ad_ratio_today`；`ensure_tables()` 尾部有 ALTER TABLE 迁移 |
| `src/server.py` L499 | `ad_ratio` 指示器加 `today` 字段（`sqlite3.Row` 不支持 `.get()`，已修正为 `row['ad_ratio_today'] if 'ad_ratio_today' in row.keys() else None`） |
| `src/server.py` L1100 | 突破 API `/api/market-health/breakouts` 返回加 `vol_ma50` |
| `scripts/daily_update.py` | 新增步骤 7.5「📰 龙虎榜+大宗+汇总」调用 `daily_review_v2.py` |
| `web/market-scan/index.html` L305 | ad_ratio 卡片双值渲染 |
| `web/market-scan/index.html` L602-611 | `openBreakoutModal` 表格加 MA50均量列，删两个重复函数定义 |
| `docs/product/大盘扫描辅助指标.md` | 全部三个指标定义已同步更新 |
| `docs/product/大盘扫描页面_产品需求书_v2.md` | 升至 v2.2，响应结构对齐实际，补充突破弹窗交互 |
| `lib/hanako_provider.py` | UZI-Skill 本地数据适配层（9个函数），K线/财务/估值/龙虎榜/两融 → 本地 SQLite |
| `skills/deep-analysis/scripts/lib/data_sources.py` | K线链顶部插入 Hanako 优先，列名中文化 + 涨跌幅 |
| `skills/deep-analysis/scripts/fetch_financials.py` | `_fetch_a_share` 顶部插入 Hanako，转 UZI 格式 |
| `scripts/daily_review_v2.py` | 产龙虎榜/大宗/两融/汇总四表（理杏仁 API） |

## 当前风险点

- `daily_review_*` 表数据只到 2026-07-10，`market-health` 日期为 2026-07-13，差了 3 天 → 资金情绪卡片（龙虎榜/大宗）为 0。`daily_update.py` 已加步骤 7.5 解决
- 放量突破中北交所股票（920/830开头）流动性稀薄易产生虚高量比，建议后续加 `amount >= 5000万` 过滤

## Next steps

1. **重启 8788**：`Get-NetTCPConnection -LocalPort 8788 | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force }`，然后 `python src/server.py`
2. **运行 ensure_tables 迁移**：启动 server.py 时自动执行，会给 DB 加 `ad_ratio_today` 和 `vol_ma50` 列
3. **重算市场健康度**：`python src/scanners/market_health.py --date 2026-07-13`（恐慌贪婪会从 75.8 变成 24.2，涨跌家数比当天值 0.17 写入 DB）
4. **跑一次日报表回填**：`python scripts/daily_review_v2.py 2026-07-13` 补上缺失的龙虎榜/大宗数据
5. **浏览器硬刷新**：F12 → Network → Disable cache → Ctrl+R
