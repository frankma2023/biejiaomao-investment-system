# Hanako 投资系统 · 项目交接文档

> 日期：2026-09-20
> 请新会话先读本文，再开始工作。

---

## 一、服务一览

| 服务 | 端口 | 当前 PID | 启动命令 |
|---|---|---|---|
| Hana API（Flask） | 8788 | 18800 | `python src\server.py` |
| 前端静态（no-cache） | 8772 | 5080 | `python scripts\serve_dev.py 8772 web` |
| DeepSeek Harness | 3080 | 20128 | 项目 `D:\hanako\deepseek-harness`，`pnpm dsh web` |

---

## 二、项目结构

| 路径 | 说明 |
|---|---|
| `D:\hanako\investment-system` | 主项目（Git: `frankma2023/biejiaomao-investment-system`） |
| `D:\hanako\deepseek-harness` | DeepSeek Harness（源码编译，pnpm） |
| `D:\hanako\.env` | 环境变量（DEEPSEEK_KEY、LIXINGER_TOKEN、GIT_TOKEN） |

---

## 三、CPA 阶段判定引擎

**当前版本**：八阶段 v3.0（2026-09-20），已全量回算

新版本将原来的 ⑥ Wedge Drop 拆为 **⑥ 初破 / ⑦ 续跌 / ⑧ 衰竭加速** 三个阶段。涉及的模块文件：

| 文件 | 说明 |
|---|---|
| `src/scanners/cpa_stage.py` | CPA 引擎核心（已更新到 v3.0） |
| `src/scanners/cpa_stage_weekly.py` | 周线级别 CPA 引擎 |
| `src/scanners/chanlun_weekly.py` | 周线级别缠论引擎 |
| `scripts/backfill_cpa_weekly.py` | 周线 CPA 回填脚本 |
| `scripts/backfill_chanlun_weekly.py` | 周线缠论回填脚本 |
| `scripts/incremental_cpa_weekly.py` | 周线 CPA 增量更新 |
| `scripts/_final_verify_weekly_cpa.py` | 周线验证脚本 |
| `scripts/_t2_regression.py` | 回归测试 |
| `scripts/_t3_weekly_verify.py` | 周线验证 |

---

## 四、深度分析页面

**版本**：V1 初版（已推送）

| 文件 | 说明 |
|---|---|
| `web/deep-analysis/index.html` | 前端页面（输入代码 → 生成 → 展示 + 下载） |
| `src/scanners/deep_analysis.py` | 引擎（数据快照 + 3 轮 LLM + job 管理） |
| `src/server.py`（+2 端点） | `POST /api/deep-analysis/start`、`GET /api/deep-analysis/status` |
| `docs/product/深度分析页面_产品需求书.md` | PRD |

分析流程：1 次整合分析 → 7 大师并行投票（LLM）→ 1 次主持人裁决，墙钟约 25 秒。

---

## 五、Git 状态

最新提交：

```
4682354  收尾：更新 progress.html
0d55beb  CPA 八阶段 v3.0：拆⑥为⑥⑦⑧，全量回算完成
f823998  深度分析页面 + API + 引擎：V1 初版
```

**当前有 39 项未提交变更**，包含大量临时脚本和调试文件。提交前需清理。

---

## 六、单元测试

```powershell
python tests\test_cpa_stage.py        # CPA 引擎测试（34 断言）
```

---

## 七、已知待办

1. **review 双轴复核**深度分析页面（按铁律，开发完成后需复核）
2. 解决 CPA 八阶段拆分的遗留问题
3. 周线级别 CPA 引擎的验证与接入 daily_update